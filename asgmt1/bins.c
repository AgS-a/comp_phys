#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{

        double array[100];

        float lowest;
        float highest;
        float bin_start = floor(lowest);
        float bin_end = ceil(highest);
        float bin_size;

        return 0;
}
